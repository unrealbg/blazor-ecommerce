namespace BuildingBlocks.Application.Contracts;

public sealed record ProductSnapshot(
    Guid Id,
    string Name,
    string Slug,
    string? ShortDescription,
    string? Description,
    string Status,
    string ProductType,
    bool IsFeatured,
    DateTime? PublishedAtUtc,
    bool IsActive,
    string? SeoTitle,
    string? SeoDescription,
    string? CanonicalUrl,
    BrandSnapshot? Brand,
    Guid? DefaultCategoryId,
    string? CategorySlug,
    string? CategoryName,
    IReadOnlyCollection<CategoryBreadcrumbSnapshot> CategoryBreadcrumbs,
    Guid DefaultVariantId,
    string Currency,
    decimal Amount,
    decimal? CompareAtAmount,
    bool IsInStock,
    bool IsTracked,
    bool AllowBackorder,
    int? AvailableQuantity,
    string? ImageUrl,
    string? Sku,
    DateTime CreatedAtUtc,
    DateTime UpdatedAtUtc,
    IReadOnlyCollection<ProductVariantSnapshot> Variants,
    IReadOnlyCollection<ProductAttributeSnapshot> Attributes,
    IReadOnlyCollection<ProductImageSnapshot> Images,
    IReadOnlyCollection<ProductRelationSnapshot> RelatedProducts);
